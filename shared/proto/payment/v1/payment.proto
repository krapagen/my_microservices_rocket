syntax = "proto3";

package payment.v1;

option go_package = "github.com/krapagen/my_microservices_rocket/shared/pkg/proto/payment/v1;paymentv1";

// PaymentService обрабатывает оплату заказов
service PaymentService {
  // PayOrder обрабатывает оплату заказа
  rpc PayOrder(PayOrderRequest) returns (PayOrderResponse);
}

// Способ оплаты
enum PaymentMethod {
  PAYMENT_METHOD_UNSPECIFIED = 0;
  PAYMENT_METHOD_CARD = 1;
  PAYMENT_METHOD_SBP = 2;
  PAYMENT_METHOD_CREDIT_CARD = 3;
  PAYMENT_METHOD_INVESTOR_MONEY = 4;
}

// Запрос на оплату заказа
message PayOrderRequest {
  // UUID заказа
  string order_uuid = 1;

  //Выбранный способ оплаты (обязательный)
  PaymentMethod payment_method = 2;
}

// Ответ на запрос транзакцией оплаты заказа
message PayOrderResponse {
  string transaction_uuid = 1; // UUID транзакции оплаты
}
